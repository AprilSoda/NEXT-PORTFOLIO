import React from 'react';

import { ReactComponent as SVG_CLOSE } from '../public/SVG_CLOSE.svg';


export const PageModal = ({ children, closeModal }) => {
  const { ModalState, setModalState } = useContext(ModalContext);


  return (
    <AnimatePreesence>


    </AnimatePreesence>
  );
}
